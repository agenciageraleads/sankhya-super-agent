#
# Dependências instaláveis via pip.
# Módulos da stdlib (re, sqlite3, typing, logging, importlib, inspect, pkgutil, etc.)
# NÃO devem ser listados aqui pois já vêm com o Python.
#
mcp
requests
python-dotenv
streamlit
plotly
pandas
google-genai
