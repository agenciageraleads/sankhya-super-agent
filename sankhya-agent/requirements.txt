#
# Dependencies installable via pip.
# Stdlib modules (re, sqlite3, typing, logging, importlib, inspect, pkgutil, etc.)
# must NOT be listed here because they are shipped with Python.
#
mcp
requests
python-dotenv
streamlit
plotly
pandas
openai
